# Screenshots

Add the following captures here so they render in the root `README.md`:

- `landing.png` — the marketing landing page
- `control-center.png` — the Agent Control Center mid-run (split view)
- `success.png` — the final success screen
- `site.png` — a generated live landing page at `/site/{id}`

Recommended width: 1600px. PNG or WebP.
